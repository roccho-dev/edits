name: run focused owned boundaries v13

on:
  push:
    branches:
      - automation/focused-owned-boundaries-20260823
    paths:
      - proofs/focused-owned-boundaries-v13.trigger

permissions:
  contents: write

concurrency:
  group: run-focused-owned-boundaries-v13
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.12'
          cache: false

      - uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            always-allow-substitutes = true

      - name: Run exact delayed popup acceptance proof
        shell: bash
        env:
          EVIDENCE: /tmp/focused-e2e-owned-boundaries-v13
        run: |
          set -euo pipefail
          cp proofs/focused-owned-boundaries-v13.sh "$RUNNER_TEMP/focused-owned-boundaries-v13.sh"
          chmod 700 "$RUNNER_TEMP/focused-owned-boundaries-v13.sh"
          git checkout --detach 1d7bf1c19f115bfad3f257b21f09f0d6bb31608a
          "$RUNNER_TEMP/focused-owned-boundaries-v13.sh"

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: focused-owned-boundaries-v13-${{ github.run_id }}
          path: /tmp/focused-e2e-owned-boundaries-v13
          compression-level: 0
          retention-days: 90
          if-no-files-found: warn
