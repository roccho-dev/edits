name: run focused owned boundaries v21

on:
  push:
    branches:
      - automation/focused-owned-boundaries-20260823
    paths:
      - proofs/run-focused-owned-boundaries-v21.trigger

permissions:
  contents: write

concurrency:
  group: focused-owned-boundaries-v21
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: automation/focused-owned-boundaries-20260823
          fetch-depth: 0
          persist-credentials: true
      - name: Preserve exact v21 helper then checkout v20 seed
        shell: bash
        run: |
          set -euo pipefail
          cp proofs/focused-owned-boundaries-v21.sh "$RUNNER_TEMP/focused-owned-boundaries-v21.sh"
          chmod 700 "$RUNNER_TEMP/focused-owned-boundaries-v21.sh"
          git checkout --detach a7c252d338342349cc352798b46f954f7de8a5a4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23.12"
          cache: false
      - uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            always-allow-substitutes = true
      - name: Run exact immutable-fixture proof
        shell: bash
        env:
          EVIDENCE: /tmp/focused-e2e-owned-boundaries-v21
        run: |
          set -euo pipefail
          "$RUNNER_TEMP/focused-owned-boundaries-v21.sh"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: focused-owned-boundaries-v21-${{ github.run_id }}
          path: /tmp/focused-e2e-owned-boundaries-v21
          compression-level: 0
          retention-days: 90
          if-no-files-found: warn
