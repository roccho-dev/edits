# Helix editor adapter

Helix smoke configuration for using `jpcmp-lsp` as a normal language server.

The editor adapter owns configuration shape only; completion behavior is owned by core and source adapters.
