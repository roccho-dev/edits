# hq-source-jsonl source adapter

Landing path for hq source JSONL candidates. It returns normalized candidates through the same provider port and is intentionally not wired into the default golden path yet.
