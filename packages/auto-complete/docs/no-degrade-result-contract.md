# no-degrade result contract

A successful run must prove:

- existing Vim candidate contract still passes;
- existing LSP smoke still passes;
- Vim adapter through Go LSP still passes;
- Helix config smoke still passes;
- new import graph, old path, provider contract, hq fixture, negative fixture, snapshot, UX audit, performance, post-merge-config, and parent checklist gates pass.
