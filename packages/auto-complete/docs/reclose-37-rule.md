# #37 re-close rule

#37 is a parent no-degrade hardening issue. It must not be closed merely because a PR was merged.

Required final evidence:

1. all blocking child issues #39-#48 are closed or explicitly moved to documented non-blocking follow-up;
2. the final post-merge workflow run on `proposals` is green;
3. the #37 thread contains the final closure comment with run id, merged PR list, and gate results.
