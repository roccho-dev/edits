# stop conditions

Stop and do not close #37 if any of these are true:

- a blocking child issue is still open without documented non-blocking rationale;
- the post-merge `proposals` workflow has no successful run;
- a snapshot was updated without review rationale;
- an adapter bypasses the provider port;
- old executable paths reappear;
- a negative fixture becomes a valid candidate.
