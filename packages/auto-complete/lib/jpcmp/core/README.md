# jpcmp core

Pure candidate extraction, rank merge, romaji projection, replace-range calculation, and completion item shaping.

Core may depend on standard library code and its own types only. It must not import JSONL adapters, LSP transport, Vim, or Helix.
