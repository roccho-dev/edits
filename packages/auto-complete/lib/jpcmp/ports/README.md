# jpcmp ports

Stable aliases for provider request and response contracts used by source adapters.

Adapters import this package so concrete source formats can be plugged into core without making core know those formats.
