# auto-complete fixtures

Fixtures in this directory are deterministic CI inputs.

- `hq.source.jsonl` proves that hq-shaped source rows can be normalized through `adapters/source/hq-source-jsonl` and exposed through the same LSP completion path.
