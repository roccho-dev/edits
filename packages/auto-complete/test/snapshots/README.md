# auto-complete snapshots

These snapshots are canonical review data. A changed candidate order, source, rank, score, filterText, sortText, textEdit range, or UX visual payload must appear as an intentional PR diff.
