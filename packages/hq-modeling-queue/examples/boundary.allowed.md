# allowed boundary fixture

`edits` owns editor command vocabulary and queue writer adapters.

`packages/hq-local-worker` is legacy/proof-only evidence and is not canonical runtime ownership.

Queue rows are intent only and do not create accepted authority.
