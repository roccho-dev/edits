# command core fixture

Command and targetRef interpretation can be pure core in edits.

Vim integration and local file append are adapters. Worker runtime, admission, accepted ledger, projection builder, and renderer stay outside edits.
