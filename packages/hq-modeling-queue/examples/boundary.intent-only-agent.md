# intent-only agent command fixture

`agent.*` command vocabulary may create `hq.agentTaskQueued.v1` intent rows.

It must not run an agent, promote a proposal, perform admission, or write accepted ledger state.
