# intent-only model command fixture

`model.*` command vocabulary may create `hq.modelCommitQueued.v1` intent rows after human confirmation.

The row is queue intent only. It is not accepted authority and it does not write an accepted ledger.
