# queue intent fixture

`edits` can write `hq.modelCommitQueued.v1` and `hq.agentTaskQueued.v1` rows as intent.

Those rows are not accepted authority, are not admission, and are not projection authority.
