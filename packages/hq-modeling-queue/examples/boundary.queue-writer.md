# queue writer fixture

The queue writer adapter may append intent rows to a local queue file.

The append operation is a side effect adapter, not accepted ledger authority and not projection authority.
