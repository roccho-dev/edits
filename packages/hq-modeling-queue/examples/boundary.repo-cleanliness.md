# repo cleanliness fixture

`edits` can contain editor and queue-writer code.

It must not contain canonical worker runtime ownership, admission ownership, accepted ledger authority, projection authority ownership, or ui renderer ownership.
