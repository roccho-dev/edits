# targetRef fixture

The editor may consume ui targetRef metadata and place it into a queue-intent row.

The targetRef is a pointer only, not an approval, not admission ownership, and not accepted ledger authority.
