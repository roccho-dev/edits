# mux port

The mux port presents a finite console and view lifecycle. It may attach,
focus, resize, snapshot within fixed bounds, and close a view. A view reference
is never a run identity or a success result.
