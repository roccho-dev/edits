# catalog adapter

Reads one immutable operation catalog and validates a closed row contract. The
adapter is pure: it performs no repository discovery, executable lookup, worker
control, or external effect.
