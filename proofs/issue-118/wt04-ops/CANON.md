# WT-04 exact ops adapter shadow RED

Purpose: connect the console to one exact released ops operation catalog, intake, result, and view-plan contract without changing the current effect path until semantic shadow parity is proved.

Merge condition: exact release/digest binding, strict catalog validation, zero pre-accept intake, one accepted intake, no direct execution/fallback, stale-result rejection, and semantic shadow equality are all Green.
